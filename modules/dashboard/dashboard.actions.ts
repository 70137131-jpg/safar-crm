"use server";

/**
 * Dashboard server actions.
 *
 * TODO (TASKS.md): each action must
 *   1. parse input with Zod,
 *   2. call requireUser() then requirePermission(),
 *   3. delegate to dashboard.service,
 *   4. return via serverAction() wrapper.
 */
export {};
