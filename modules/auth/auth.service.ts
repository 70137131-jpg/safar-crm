/**
 * Auth service.
 *
 * TODO (TASKS.md): implement business logic for the auth module.
 * Services own orchestration + audit; repositories own DB calls.
 * Never read cookies()/headers() here — accept a UserContext arg.
 */
export {};
