// Test stub for the `server-only` package, which throws when imported outside
// a React Server Component bundle. Aliased in vitest.config.ts so that
// server-only modules (e.g. dashboard scope) can be unit-tested in Node.
export {};
